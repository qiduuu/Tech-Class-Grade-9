function start(){
	var name = readLine("What's your name? ");
	var dinnerTime = readInt("What hour are we meeting for dinner? ");
	
	println("Ok, " + name + ".\nWe will meet at " + dinnerTime + ".");
}